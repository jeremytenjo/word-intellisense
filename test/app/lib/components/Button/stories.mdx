import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import Button from './'

<Meta
  title='lib/components/Button'
  component={Button}
  argTypes={{
    size: {
      options: ['normal', 'big'],
    },
  }}
  args={{ text: 'Hello Button' }}
/>

# Button

export const Template = (args) => <Button {...args} />

<Canvas>
  <Story name='default' argTypes={{}} args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
